[project]
name = "performance-os-worker"
version = "0.1.0"
description = "Background workers for Performance OS sync and import pipelines"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
